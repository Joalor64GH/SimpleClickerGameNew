function create() {
    trace('script initialized!');
}

function createPost() {
    // after create is called
}

function update(elapsed:Float) {
    // while the game is updating
}

function updatePost(elapsed:Float) {
    // after update is called
}

function destroy() {
    // when leaving playstate
}